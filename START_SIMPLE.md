# Простой запуск бота (для хостингов без SSH команд)

## ✅ Что уже встроено в бота:

1. **Автоперезапуск при ошибках** - бот сам перезапустится если упадет
2. **Таймауты на парсинг** - не зависнет навсегда
3. **Встроенный Watchdog** - проверяет память и CPU каждые 5 минут
4. **Защита от зависания Playwright** - автопереключение на Simple парсер
5. **Парсинг 1 раз в сутки (00:00)** - минимальная нагрузка

---

## 🚀 Как запустить бота (3 простых шага):

### Шаг 1: Загрузите файлы на хостинг

Загрузите всю папку `host/` на ваш хостинг через FTP, панель управления и т.д.

### Шаг 2: Установите зависимости

В панели хостинга найдите консоль/терминал и выполните:
```bash
pip install -r requirements.txt
```

ИЛИ (если pip3):
```bash
pip3 install -r requirements.txt
```

### Шаг 3: Запустите бота

**Вариант А: Если есть кнопка "Запуск Python скрипта":**
- Выберите файл: `main.py`
- Нажмите "Запустить"
- Готово! ✅

**Вариант Б: Если есть консоль:**
```bash
python3 main.py
```

**Вариант В: Если есть screen:**
```bash
screen -dmS promobot python3 main.py
```

**Вариант Г: Если есть nohup:**
```bash
nohup python3 main.py > bot.log 2>&1 &
```

---

## 📊 Как проверить что бот работает:

### 1. Проверьте логи:

**Если запускали через панель хостинга:**
- Найдите папку `logs/`
- Откройте файл `bot_2025-12-11.log` (с сегодняшней датой)
- Должны увидеть: `PromoBot успешно запущен!`

**Если запускали через консоль:**
```bash
tail -f logs/bot_$(date +%Y-%m-%d).log
```

### 2. Напишите боту в Telegram:
- Напишите `/start`
- Бот должен ответить

### 3. Проверьте watchdog:
В логах каждые 5 минут должна быть запись:
```
✅ Watchdog: Memory=XXX MB, CPU=X%, Uptime=X.Xh
```

---

## 🛡️ Защита от выключения:

### Что делает бот автоматически:

1. **Если зависнет Playwright (30 сек):**
   - ✅ Переключится на Simple парсер
   - ✅ Продолжит работу

2. **Если парсинг затянется (15 минут):**
   - ✅ Остановит парсинг принудительно
   - ✅ Продолжит работу

3. **Если Telegram API не отвечает:**
   - ✅ Подождет 1 секунду
   - ✅ Попробует еще раз
   - ✅ При критической ошибке - перезапустится

4. **Если произойдет любая ошибка:**
   - ✅ Залогирует ошибку
   - ✅ Подождет 5 секунд
   - ✅ Перезапустится автоматически

5. **Watchdog проверяет каждые 5 минут:**
   - ✅ Память (предупреждение если >500MB)
   - ✅ CPU (предупреждение если >80%)
   - ✅ Время работы

---

## ⚠️ Что может остановить бота:

| Событие | Что будет | Как избежать |
|---------|-----------|--------------|
| Перезагрузка хостинга | Бот остановится | Настроить автозапуск в панели |
| Процесс убит вручную | Бот остановится | Не убивать процесс :) |
| Закончилось место на диске | Бот остановится | Очищать старые логи |
| Закончилась память (OOM) | Бот остановится | Следить за логами watchdog |

---

## 🔧 Настройка автозапуска (если есть панель):

### Многие хостинги имеют функцию автозапуска:

**Timeweb / Beget / и другие:**
1. Зайдите в "Планировщик задач" или "Автозапуск"
2. Добавьте команду: `python3 /полный/путь/к/main.py`
3. Установите: "При загрузке системы"

**Heroku / Railway / Render:**
- Создайте файл `Procfile`:
  ```
  worker: python main.py
  ```

**PythonAnywhere:**
- Зайдите в "Web" → "Add a new web app"
- Выберите: "Manual configuration"
- В настройках укажите: `main.py`

---

## 📱 Мониторинг через Telegram:

Бот будет отправлять вам уведомления:
- ✅ При запуске: "PromoBot запущен!"
- ⚠️ При остановке: "PromoBot остановлен"
- 📊 Админ-панель: `/admin` - статистика работы

---

## 🆘 Если бот всё равно падает:

### 1. Проверьте последние логи:
Откройте `logs/bot_YYYY-MM-DD.log` и посмотрите самую последнюю ошибку

### 2. Частые проблемы:

**"No module named 'psutil'"**
- Решение: `pip install psutil`

**"Permission denied"**
- Решение: Проверьте права доступа к папке

**"Address already in use"**
- Решение: Бот уже запущен, остановите старый процесс

**Большое использование памяти**
- Решение: Уменьшите `max_shops` в парсере (в `cuponcod_parser.py`)

---

## ✅ Итоговая проверка:

- [ ] Бот запущен
- [ ] Логи пишутся в `logs/`
- [ ] Бот отвечает на `/start` в Telegram
- [ ] Watchdog работает (видно в логах каждые 5 минут)
- [ ] Парсинг запланирован на 00:00 (видно в логах)

**Если все пункты ✅ - бот работает стабильно!**

---

## 📞 Дополнительная помощь:

Если возникли проблемы:
1. Сохраните последние 100 строк из логов
2. Проверьте `.env` файл (правильный ли токен бота)
3. Проверьте доступ к интернету на хостинге
