# Настройка автоматического мониторинга бота

## Что делает monitor_bot.sh:

✅ Проверяет, запущен ли бот каждые 5 минут
✅ Автоматически перезапускает если бот упал
✅ Проверяет использование памяти (предупреждение если > 500MB)
✅ Проверяет свежесть логов (перезапуск если нет активности 10 минут)
✅ Логирует все действия в `logs/monitor.log`

---

## Установка автомониторинга

### 1. Сделайте скрипт исполняемым:
```bash
chmod +x monitor_bot.sh
```

### 2. Протестируйте скрипт вручную:
```bash
./monitor_bot.sh
```

Должен вывести что-то вроде:
```
[2025-12-11 12:00:00] ✅ Бот успешно запущен
```

### 3. Добавьте в crontab (автозапуск каждые 5 минут):

```bash
crontab -e
```

Добавьте в конец файла:
```cron
# Мониторинг PromoBot каждые 5 минут
*/5 * * * * /path/to/promo-bot/host/monitor_bot.sh >> /path/to/promo-bot/host/logs/cron.log 2>&1
```

**Замените** `/path/to/promo-bot/host` на **полный путь** к вашей папке!

Пример:
```cron
*/5 * * * * /home/user/promo-bot/host/monitor_bot.sh >> /home/user/promo-bot/host/logs/cron.log 2>&1
```

### 4. Сохраните и закройте (Ctrl+X, затем Y, затем Enter)

### 5. Проверьте что cron работает:
```bash
crontab -l
```

Должны увидеть добавленную строку.

---

## Проверка работы мониторинга

### Просмотр логов мониторинга:
```bash
tail -f logs/monitor.log
```

### Просмотр логов cron:
```bash
tail -f logs/cron.log
```

### Тест: остановите бота и подождите 5 минут
```bash
./stop_bot.sh

# Подождите 5 минут...
# Скрипт мониторинга должен автоматически запустить бота

./status_bot.sh
```

---

## Альтернатива: запуск мониторинга вручную (без cron)

Если у вас нет доступа к cron, запустите мониторинг в отдельной screen сессии:

```bash
screen -dmS bot_monitor bash -c 'while true; do ./monitor_bot.sh; sleep 300; done'
```

Просмотр:
```bash
screen -r bot_monitor
```

Выход (не останавливая): `Ctrl+A`, затем `D`

Остановка:
```bash
screen -S bot_monitor -X quit
```

---

## Уровни защиты бота

### Уровень 1: Автоперезапуск внутри кода (УЖЕ ЕСТЬ ✅)
- Бот автоматически перезапускается при сетевых ошибках
- Таймаут на Playwright: 30 секунд
- Таймаут на парсинг: 15 минут
- Автоматическое переключение на Simple парсер

### Уровень 2: Monitor script (monitor_bot.sh)
- Проверка каждые 5 минут через cron
- Перезапуск если процесс упал
- Проверка активности (логи)

### Уровень 3: Systemd (для серверов)
- Автозапуск при загрузке системы
- Автоматический перезапуск при падении
- Управление через systemctl

---

## Что делать если бот всё равно падает

### 1. Проверьте логи:
```bash
# Логи бота
tail -100 logs/bot_$(date +%Y-%m-%d).log

# Логи мониторинга
tail -100 logs/monitor.log

# Системные логи (если systemd)
sudo journalctl -u promobot -n 100
```

### 2. Проверьте использование ресурсов:
```bash
# Память
free -h

# Диск
df -h

# Процессы
htop
```

### 3. Проверьте сетевое соединение:
```bash
# Ping Telegram API
ping -c 5 api.telegram.org

# Проверка DNS
nslookup api.telegram.org
```

### 4. Увеличьте задержку перед перезапуском в main.py:
```python
restart_delay = 10  # Вместо 5 секунд
```

### 5. Добавьте больше логирования:
В `config.py` измените уровень логов:
```python
LOG_LEVEL = "DEBUG"  # Вместо "INFO"
```

---

## Примеры логов мониторинга

### Нормальная работа:
```
[2025-12-11 12:00:00] Бот работает (PID: 12345, память: 234MB)
[2025-12-11 12:05:00] Бот работает (PID: 12345, память: 236MB)
```

### Автоматический перезапуск:
```
[2025-12-11 12:10:00] ❌ Бот не запущен! Запускаем...
[2025-12-11 12:10:10] ✅ Бот успешно запущен
```

### Проблема с памятью:
```
[2025-12-11 12:15:00] ⚠️ WARNING: Высокое использование памяти: 567MB
```

### Бот завис (нет логов):
```
[2025-12-11 12:20:00] ⚠️ WARNING: Нет логов за последние 10 минут - возможно бот завис
[2025-12-11 12:20:00] 🔄 Перезапускаем бота...
[2025-12-11 12:20:15] ✅ Бот перезапущен
```

---

## Полезные команды

```bash
# Просмотр всех процессов бота
ps aux | grep python

# Убить все процессы бота (аварийная остановка)
pkill -9 -f "python.*main.py"

# Очистка логов старше 7 дней
find logs/ -name "*.log" -mtime +7 -delete

# Размер логов
du -sh logs/

# Список задач cron
crontab -l

# Удалить задачу из cron
crontab -e  # Удалите строку и сохраните
```
